// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider        = "prisma-client-js"
  previewFeatures = ["referentialIntegrity"]
}

datasource db {
  provider             = "mysql"
  url                  = "mysql://wx479bz4gfuj:pscale_pw_15zQE432vUwBbU80XPRCVUjFwBvS1wGdpg8bxK9EmgE@d1acbowmn3oi.us-west-2.psdb.cloud/hottest-dev?sslaccept=strict"
  shadowDatabaseUrl    = "mysql://6w47vzr2y6vq:pscale_pw_5DvPHDl3c8u_PdQbXGusjKgy_vTI2df2avFADilmmws@d1acbowmn3oi.us-west-2.psdb.cloud/hottest-shadow?sslaccept=strict"
  referentialIntegrity = "prisma"
}

model Vote {
  id        String   @id @default(cuid())
  createdAt DateTime @default(now())

  votedFor       Nft    @relation(name: "votesFor", fields: [votedForId], references: [id])
  votedForId     Int
  votedAgainst   Nft    @relation(name: "votesAgainst", fields: [votedAgainstId], references: [id])
  votedAgainstId Int

  @@index([votedForId])
  @@index([votedAgainstId])
}

model Nft {
  id        Int   @id @default(autoincrement())
  createdAt DateTime @default(now())

  name            String? @default("Not an actual NFT")
  imageUrl        String? @db.VarChar(3000) @default("https://lh3.googleusercontent.com/BZplkkrAXupi_CAGBHSy15DCykNV1ifK2WQymvfctHqIClvyucDXoyONGaLOcxjzm_xc6eW5GrXKb-XQ3U_vHbVHGzQUxPT48cqh=w600")
  contractAddress String? @default("0x0000000000000000000000000000000000000000")
  owner           String? @default("Alice and Bob") //owner will end up just being the wallet address from React.useState
  votedFor        Vote[]  @relation(name: "votesFor")
  votedAgainst    Vote[]  @relation(name: "votesAgainst")
}
